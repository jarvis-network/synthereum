{
  "name": "@jarvis-network/synthereum-contracts",
  "version": "1.0.0",
  "description": "",
  "files": [
    "dist"
  ],
  "types": "dist",
  "scripts": {
    "clean": "git clean -fdx -e .env -e node_modules -- .",
    "format": "prettier --write .",
    "format:sol": "prettier --write './contracts/**/*.sol'",
    "start": "yarn build",
    "watch:tsc": "tsc -p ./tsconfig.json -w --preserveWatchOutput",
    "build": "yarn build:solidity && yarn build:typechain && yarn build:typechain-index && yarn build:tsc",
    "build:solidity": "truffle compile",
    "build:typechain": "typechain --target web3-v1 --outDir ./src/contracts/typechain './build/contracts/*.json' | { head -n 2; tail -n1; }",
    "build:typechain-index": "sol2ts-code-gen ./src/contracts",
    "build:tsc": "tsc -p ./tsconfig.json",
    "migrate-finder": "MIGRATION_TYPE=finder hardhat test ./test/correct-migration.js",
    "migrate-deployer": "MIGRATION_TYPE=deployer hardhat test ./test/correct-migration.js",
    "migrate-pool-registry": "MIGRATION_TYPE=poolRegistry hardhat test ./test/correct-migration.js",
    "migrate-factory-versioning": "MIGRATION_TYPE=factoryVersioning hardhat test ./test/correct-migration.js",
    "migrate-manager": "MIGRATION_TYPE=manager hardhat test ./test/correct-migration.js",
    "migrate-derivative-version": "MIGRATION_TYPE=derivativeVersions hardhat test ./test/correct-migration.js",
    "migrate-pool-version": "MIGRATION_TYPE=poolVersions hardhat test ./test/correct-migration.js",
    "migrate-price-feed": "MIGRATION_TYPE=priceFeed hardhat test ./test/correct-migration.js",
    "migrate-all": "hardhat test ./test/correct-migration.js",
    "add-pool-and-derivative": "MIGRATION_TYPE=pool-and-derivative hardhat test ./test/correct-migration.js",
    "add-only-pool": "MIGRATION_TYPE=only-pool hardhat test ./test/correct-migration.js",
    "add-only-derivative": "MIGRATION_TYPE=only-derivative hardhat test ./test/correct-migration.js",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "verify": "./utils/verify.d",
    "start:local-fork": "./local-fork.sh"
  },
  "author": "Alessandro Bergamaschi",
  "license": "ISC",
  "dependencies": {
    "@jarvis-network/uma-common": "0.3.11",
    "@jarvis-network/uma-core": "0.3.11",
    "@jarvis-network/core-utils": "*",
    "@openzeppelin/contracts": "3.3.0",
    "web3": "^1.3.4"
  },
  "devDependencies": {
    "@jarvis-network/sol2ts-code-gen": "*",
    "@typechain/web3-v1": "^1.0.0",
    "ethers": "^5.0.26",
    "ganache-cli": "^6.12.1",
    "ganache-time-traveler": "^1.0.15",
    "secp256k1": "^4.0.2",
    "truffle": "5.1.49",
    "truffle-assertions": "^0.9.2",
    "typechain": "^4.0.1"
  }
}
