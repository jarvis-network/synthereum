/* File autogenerated by synthereum-lib. Do not edit manually. */

import type { AbiItem } from 'web3-utils';
import type { Tagged } from '@jarvis-network/web3-utils/base/tagged-type';

import type { AccessControl } from '../typechain/AccessControl';
export const AccessControl_Abi = require('./AccessControl.json') as Tagged<
  AbiItem[],
  AccessControl
>;

import type { AddressWhitelist } from '../typechain/AddressWhitelist';
export const AddressWhitelist_Abi = require('./AddressWhitelist.json').abi as Tagged<
  AbiItem[],
  AddressWhitelist
>;

import type { AdministrateeInterface } from '../typechain/AdministrateeInterface';
export const AdministrateeInterface_Abi = require('./AdministrateeInterface.json').abi as Tagged<
  AbiItem[],
  AdministrateeInterface
>;

import type { ContractCreator } from '../typechain/ContractCreator';
export const ContractCreator_Abi = require('./ContractCreator.json').abi as Tagged<
  AbiItem[],
  ContractCreator
>;

import type { DesignatedVoting } from '../typechain/DesignatedVoting';
export const DesignatedVoting_Abi = require('./DesignatedVoting.json').abi as Tagged<
  AbiItem[],
  DesignatedVoting
>;

import type { DesignatedVotingFactory } from '../typechain/DesignatedVotingFactory';
export const DesignatedVotingFactory_Abi = require('./DesignatedVotingFactory.json').abi as Tagged<
  AbiItem[],
  DesignatedVotingFactory
>;

import type { ERC20 } from '../typechain/ERC20';
export const ERC20_Abi = require('./ERC20.json') as Tagged<AbiItem[], ERC20>;

import type { ERC20Snapshot } from '../typechain/ERC20Snapshot';
export const ERC20Snapshot_Abi = require('./ERC20Snapshot.json').abi as Tagged<
  AbiItem[],
  ERC20Snapshot
>;

import type { ExpandedERC20 } from '../typechain/ExpandedERC20';
export const ExpandedERC20_Abi = require('./ExpandedERC20.json').abi as Tagged<
  AbiItem[],
  ExpandedERC20
>;

import type { ExpandedIERC20 } from '../typechain/ExpandedIERC20';
export const ExpandedIERC20_Abi = require('./ExpandedIERC20.json').abi as Tagged<
  AbiItem[],
  ExpandedIERC20
>;

import type { FeePayerPoolParty } from '../typechain/FeePayerPoolParty';
export const FeePayerPoolParty_Abi = require('./FeePayerPoolParty.json').abi as Tagged<
  AbiItem[],
  FeePayerPoolParty
>;

import type { FeePayerPoolPartyLib } from '../typechain/FeePayerPoolPartyLib';
export const FeePayerPoolPartyLib_Abi = require('./FeePayerPoolPartyLib.json').abi as Tagged<
  AbiItem[],
  FeePayerPoolPartyLib
>;

import type { FinancialContractsAdmin } from '../typechain/FinancialContractsAdmin';
export const FinancialContractsAdmin_Abi = require('./FinancialContractsAdmin.json').abi as Tagged<
  AbiItem[],
  FinancialContractsAdmin
>;

import type { Finder } from '../typechain/Finder';
export const Finder_Abi = require('./Finder.json').abi as Tagged<
  AbiItem[],
  Finder
>;

import type { FinderInterface } from '../typechain/FinderInterface';
export const FinderInterface_Abi = require('./FinderInterface.json') as Tagged<
  AbiItem[],
  FinderInterface
>;

import type { Governor } from '../typechain/Governor';
export const Governor_Abi = require('./Governor.json').abi as Tagged<
  AbiItem[],
  Governor
>;

import type { HitchensUnorderedKeySet } from '../typechain/HitchensUnorderedKeySet';
export const HitchensUnorderedKeySet_Abi = require('./HitchensUnorderedKeySet.json') as Tagged<
  AbiItem[],
  HitchensUnorderedKeySet
>;

import type { IDeploymentSignature } from '../typechain/IDeploymentSignature';
export const IDeploymentSignature_Abi = require('./IDeploymentSignature.json').abi as Tagged<
  AbiItem[],
  IDeploymentSignature
>;

import type { IDerivative } from '../typechain/IDerivative';
export const IDerivative_Abi = require('./IDerivative.json') as Tagged<
  AbiItem[],
  IDerivative
>;

import type { IDerivativeDeployment } from '../typechain/IDerivativeDeployment';
export const IDerivativeDeployment_Abi = require('./IDerivativeDeployment.json') as Tagged<
  AbiItem[],
  IDerivativeDeployment
>;

import type { IERC20 } from '../typechain/IERC20';
export const IERC20_Abi = require('./IERC20.json') as Tagged<AbiItem[], IERC20>;

import type { IERC20Standard } from '../typechain/IERC20Standard';
export const IERC20Standard_Abi = require('./IERC20Standard.json').abi as Tagged<
  AbiItem[],
  IERC20Standard
>;

import type { IRole } from '../typechain/IRole';
export const IRole_Abi = require('./IRole.json').abi as Tagged<
  AbiItem[],
  IRole
>;

import type { IStandardERC20 } from '../typechain/IStandardERC20';
export const IStandardERC20_Abi = require('./IStandardERC20.json') as Tagged<
  AbiItem[],
  IStandardERC20
>;

import type { ISynthereumDeployer } from '../typechain/ISynthereumDeployer';
export const ISynthereumDeployer_Abi = require('./ISynthereumDeployer.json').abi as Tagged<
  AbiItem[],
  ISynthereumDeployer
>;

import type { ISynthereumFactoryVersioning } from '../typechain/ISynthereumFactoryVersioning';
export const ISynthereumFactoryVersioning_Abi = require('./ISynthereumFactoryVersioning.json').abi as Tagged<
  AbiItem[],
  ISynthereumFactoryVersioning
>;

import type { ISynthereumFinder } from '../typechain/ISynthereumFinder';
export const ISynthereumFinder_Abi = require('./ISynthereumFinder.json').abi as Tagged<
  AbiItem[],
  ISynthereumFinder
>;

import type { ISynthereumPool } from '../typechain/ISynthereumPool';
export const ISynthereumPool_Abi = require('./ISynthereumPool.json').abi as Tagged<
  AbiItem[],
  ISynthereumPool
>;

import type { ISynthereumPoolDeployment } from '../typechain/ISynthereumPoolDeployment';
export const ISynthereumPoolDeployment_Abi = require('./ISynthereumPoolDeployment.json').abi as Tagged<
  AbiItem[],
  ISynthereumPoolDeployment
>;

import type { ISynthereumPoolRegistry } from '../typechain/ISynthereumPoolRegistry';
export const ISynthereumPoolRegistry_Abi = require('./ISynthereumPoolRegistry.json').abi as Tagged<
  AbiItem[],
  ISynthereumPoolRegistry
>;

import type { IdentifierWhitelist } from '../typechain/IdentifierWhitelist';
export const IdentifierWhitelist_Abi = require('./IdentifierWhitelist.json').abi as Tagged<
  AbiItem[],
  IdentifierWhitelist
>;

import type { IdentifierWhitelistInterface } from '../typechain/IdentifierWhitelistInterface';
export const IdentifierWhitelistInterface_Abi = require('./IdentifierWhitelistInterface.json').abi as Tagged<
  AbiItem[],
  IdentifierWhitelistInterface
>;

import type { Migrations } from '../typechain/Migrations';
export const Migrations_Abi = require('./Migrations.json') as Tagged<
  AbiItem[],
  Migrations
>;

import type { MintableBurnableERC20 } from '../typechain/MintableBurnableERC20';
export const MintableBurnableERC20_Abi = require('./MintableBurnableERC20.json').abi as Tagged<
  AbiItem[],
  MintableBurnableERC20
>;

import type { MintableBurnableIERC20 } from '../typechain/MintableBurnableIERC20';
export const MintableBurnableIERC20_Abi = require('./MintableBurnableIERC20.json').abi as Tagged<
  AbiItem[],
  MintableBurnableIERC20
>;

import type { MintableBurnableSyntheticToken } from '../typechain/MintableBurnableSyntheticToken';
export const MintableBurnableSyntheticToken_Abi = require('./MintableBurnableSyntheticToken.json').abi as Tagged<
  AbiItem[],
  MintableBurnableSyntheticToken
>;

import type { MintableBurnableTokenFactory } from '../typechain/MintableBurnableTokenFactory';
export const MintableBurnableTokenFactory_Abi = require('./MintableBurnableTokenFactory.json').abi as Tagged<
  AbiItem[],
  MintableBurnableTokenFactory
>;

import type { MultiRole } from '../typechain/MultiRole';
export const MultiRole_Abi = require('./MultiRole.json').abi as Tagged<
  AbiItem[],
  MultiRole
>;

import type { OptimisticOracle } from '../typechain/OptimisticOracle';
export const OptimisticOracle_Abi = require('./OptimisticOracle.json').abi as Tagged<
  AbiItem[],
  OptimisticOracle
>;

import type { OptimisticOracleInterface } from '../typechain/OptimisticOracleInterface';
export const OptimisticOracleInterface_Abi = require('./OptimisticOracleInterface.json').abi as Tagged<
  AbiItem[],
  OptimisticOracleInterface
>;

import type { OptimisticRequester } from '../typechain/OptimisticRequester';
export const OptimisticRequester_Abi = require('./OptimisticRequester.json').abi as Tagged<
  AbiItem[],
  OptimisticRequester
>;

import type { OracleAncillaryInterface } from '../typechain/OracleAncillaryInterface';
export const OracleAncillaryInterface_Abi = require('./OracleAncillaryInterface.json').abi as Tagged<
  AbiItem[],
  OracleAncillaryInterface
>;

import type { OracleInterface } from '../typechain/OracleInterface';
export const OracleInterface_Abi = require('./OracleInterface.json').abi as Tagged<
  AbiItem[],
  OracleInterface
>;

import type { OracleInterfaces } from '../typechain/OracleInterfaces';
export const OracleInterfaces_Abi = require('./OracleInterfaces.json').abi as Tagged<
  AbiItem[],
  OracleInterfaces
>;

import type { Ownable } from '../typechain/Ownable';
export const Ownable_Abi = require('./Ownable.json').abi as Tagged<
  AbiItem[],
  Ownable
>;

import type { PerpetualLiquidatablePoolParty } from '../typechain/PerpetualLiquidatablePoolParty';
export const PerpetualLiquidatablePoolParty_Abi = require('./PerpetualLiquidatablePoolParty.json').abi as Tagged<
  AbiItem[],
  PerpetualLiquidatablePoolParty
>;

import type { PerpetualLiquidatablePoolPartyLib } from '../typechain/PerpetualLiquidatablePoolPartyLib';
export const PerpetualLiquidatablePoolPartyLib_Abi = require('./PerpetualLiquidatablePoolPartyLib.json').abi as Tagged<
  AbiItem[],
  PerpetualLiquidatablePoolPartyLib
>;

import type { PerpetualPoolParty } from '../typechain/PerpetualPoolParty';
export const PerpetualPoolParty_Abi = require('./PerpetualPoolParty.json').abi as Tagged<
  AbiItem[],
  PerpetualPoolParty
>;

import type { PerpetualPoolPartyCreator } from '../typechain/PerpetualPoolPartyCreator';
export const PerpetualPoolPartyCreator_Abi = require('./PerpetualPoolPartyCreator.json').abi as Tagged<
  AbiItem[],
  PerpetualPoolPartyCreator
>;

import type { PerpetualPositionManagerPoolParty } from '../typechain/PerpetualPositionManagerPoolParty';
export const PerpetualPositionManagerPoolParty_Abi = require('./PerpetualPositionManagerPoolParty.json').abi as Tagged<
  AbiItem[],
  PerpetualPositionManagerPoolParty
>;

import type { PerpetualPositionManagerPoolPartyLib } from '../typechain/PerpetualPositionManagerPoolPartyLib';
export const PerpetualPositionManagerPoolPartyLib_Abi = require('./PerpetualPositionManagerPoolPartyLib.json').abi as Tagged<
  AbiItem[],
  PerpetualPositionManagerPoolPartyLib
>;

import type { Registry } from '../typechain/Registry';
export const Registry_Abi = require('./Registry.json').abi as Tagged<
  AbiItem[],
  Registry
>;

import type { RegistryInterface } from '../typechain/RegistryInterface';
export const RegistryInterface_Abi = require('./RegistryInterface.json').abi as Tagged<
  AbiItem[],
  RegistryInterface
>;

import type { Store } from '../typechain/Store';
export const Store_Abi = require('./Store.json').abi as Tagged<
  AbiItem[],
  Store
>;

import type { StoreInterface } from '../typechain/StoreInterface';
export const StoreInterface_Abi = require('./StoreInterface.json').abi as Tagged<
  AbiItem[],
  StoreInterface
>;

import type { SynthereumDeployer } from '../typechain/SynthereumDeployer';
export const SynthereumDeployer_Abi = require('./SynthereumDeployer.json').abi as Tagged<
  AbiItem[],
  SynthereumDeployer
>;

import type { SynthereumDerivativeFactory } from '../typechain/SynthereumDerivativeFactory';
export const SynthereumDerivativeFactory_Abi = require('./SynthereumDerivativeFactory.json').abi as Tagged<
  AbiItem[],
  SynthereumDerivativeFactory
>;

import type { SynthereumFactoryVersioning } from '../typechain/SynthereumFactoryVersioning';
export const SynthereumFactoryVersioning_Abi = require('./SynthereumFactoryVersioning.json').abi as Tagged<
  AbiItem[],
  SynthereumFactoryVersioning
>;

import type { SynthereumFinder } from '../typechain/SynthereumFinder';
export const SynthereumFinder_Abi = require('./SynthereumFinder.json').abi as Tagged<
  AbiItem[],
  SynthereumFinder
>;

import type { SynthereumInterfaces } from '../typechain/SynthereumInterfaces';
export const SynthereumInterfaces_Abi = require('./SynthereumInterfaces.json').abi as Tagged<
  AbiItem[],
  SynthereumInterfaces
>;

import type { SynthereumPool } from '../typechain/SynthereumPool';
export const SynthereumPool_Abi = require('./SynthereumPool.json').abi as Tagged<
  AbiItem[],
  SynthereumPool
>;

import type { SynthereumPoolCreator } from '../typechain/SynthereumPoolCreator';
export const SynthereumPoolCreator_Abi = require('./SynthereumPoolCreator.json').abi as Tagged<
  AbiItem[],
  SynthereumPoolCreator
>;

import type { SynthereumPoolFactory } from '../typechain/SynthereumPoolFactory';
export const SynthereumPoolFactory_Abi = require('./SynthereumPoolFactory.json').abi as Tagged<
  AbiItem[],
  SynthereumPoolFactory
>;

import type { SynthereumPoolLib } from '../typechain/SynthereumPoolLib';
export const SynthereumPoolLib_Abi = require('./SynthereumPoolLib.json').abi as Tagged<
  AbiItem[],
  SynthereumPoolLib
>;

import type { SynthereumPoolRegistry } from '../typechain/SynthereumPoolRegistry';
export const SynthereumPoolRegistry_Abi = require('./SynthereumPoolRegistry.json').abi as Tagged<
  AbiItem[],
  SynthereumPoolRegistry
>;

import type { SynthereumSyntheticTokenFactory } from '../typechain/SynthereumSyntheticTokenFactory';
export const SynthereumSyntheticTokenFactory_Abi = require('./SynthereumSyntheticTokenFactory.json').abi as Tagged<
  AbiItem[],
  SynthereumSyntheticTokenFactory
>;

import type { SynthereumTIC } from '../typechain/SynthereumTIC';
export const SynthereumTIC_Abi = require('./SynthereumTIC.json') as Tagged<
  AbiItem[],
  SynthereumTIC
>;

import type { SynthereumTICFactory } from '../typechain/SynthereumTICFactory';
export const SynthereumTICFactory_Abi = require('./SynthereumTICFactory.json').abi as Tagged<
  AbiItem[],
  SynthereumTICFactory
>;

import type { SynthereumTICInterface } from '../typechain/SynthereumTICInterface';
export const SynthereumTICInterface_Abi = require('./SynthereumTICInterface.json') as Tagged<
  AbiItem[],
  SynthereumTICInterface
>;

import type { TICCreator } from '../typechain/TICCreator';
export const TICCreator_Abi = require('./TICCreator.json') as Tagged<
  AbiItem[],
  TICCreator
>;

import type { Testable } from '../typechain/Testable';
export const Testable_Abi = require('./Testable.json').abi as Tagged<
  AbiItem[],
  Testable
>;

import type { TestnetERC20 } from '../typechain/TestnetERC20';
export const TestnetERC20_Abi = require('./TestnetERC20.json').abi as Tagged<
  AbiItem[],
  TestnetERC20
>;

import type { Timer } from '../typechain/Timer';
export const Timer_Abi = require('./Timer.json').abi as Tagged<
  AbiItem[],
  Timer
>;

import type { TokenMigrator } from '../typechain/TokenMigrator';
export const TokenMigrator_Abi = require('./TokenMigrator.json').abi as Tagged<
  AbiItem[],
  TokenMigrator
>;

import type { Voting } from '../typechain/Voting';
export const Voting_Abi = require('./Voting.json').abi as Tagged<
  AbiItem[],
  Voting
>;

import type { VotingAncillaryInterface } from '../typechain/VotingAncillaryInterface';
export const VotingAncillaryInterface_Abi = require('./VotingAncillaryInterface.json').abi as Tagged<
  AbiItem[],
  VotingAncillaryInterface
>;

import type { VotingInterface } from '../typechain/VotingInterface';
export const VotingInterface_Abi = require('./VotingInterface.json').abi as Tagged<
  AbiItem[],
  VotingInterface
>;

import type { VotingToken } from '../typechain/VotingToken';
export const VotingToken_Abi = require('./VotingToken.json').abi as Tagged<
  AbiItem[],
  VotingToken
>;

import type { Withdrawable } from '../typechain/Withdrawable';
export const Withdrawable_Abi = require('./Withdrawable.json').abi as Tagged<
  AbiItem[],
  Withdrawable
>;
